# Changelog

## v3.15.0
- グループのエクスポート/インポート機能を追加(チーム共有・バックアップ用)
  - ⚙パネル下部からJSONファイルとして書き出し/取り込み
  - インポートは追加マージ方式: 既存グループはそのまま、同名グループはメンバーを上書き
  - ピン留めも含めて書き出し。保存失敗時は取り込み前の状態に戻す

## v3.14.0
- 初回オンボーディングをコーチマーク(対象を指す吹き出し)に刷新
  - 初回: バー紹介 / 初グループ作成後: ドラッグ移動の案内
  - 目的のアクション実行で自動クローズ、ドラッグ・リサイズに追従

## v3.13.x
- ピン留め(常に表示)機能: 📌したカレンダーはグループOFF・排他モードの対象外に
- ピン留めのTipsを追加(Tipsは1度に1つずつ順番に表示)

## v3.12.x
- **仮想スクロール対応**: サイドバーが画面内の行しかDOMに描画しない問題に対応
  - グループ切替時、画面外のメンバーはサイドバーを自動スクロールして確実に適用
  - 「非表示中」判定を「最終検出の名前リスト」ベースに変更し誤バッジを解消
- 切替完了時のチップ表示を即時反映(300msの表示空白を解消)

## v3.9.0 - v3.11.0
- カレンダーの自動検出(エディタを開いた際、未検出または24時間経過で自動実行)
- エディタ表示中に新しいカレンダーを検知した際の更新バナー
- 初回Tips(リストの仕組みと再検出の案内)

## v3.6.0 - v3.8.x
- 自動スキャン: サイドバーを自動スクロールして全カレンダーを検出
- 検出状態の可視化(候補件数・最終検出日時・ステータス行)
- 絞り込み0件時のエンプティステート(再検出への導線)
- 非表示中カレンダーの候補からの個別削除(×)

## v3.2.0 - v3.5.0
- 選択意図ベースの状態管理(グループ間でカレンダーが重複しても選択が独立)
- 「グループを重ねて表示」トグル(OFFで1グループずつの排他切替)
- UX改善一式: 切替中のビジー表示、✓/−の状態記号、aria-pressed、
  トースト通知、インライン追加・名前変更、2段階削除、絞り込み検索
- バーの「+」を廃止し、グループ0件時のみ「＋ グループを作成」チップを表示

## v3.0.0
- UIを画面上部のフローティングチップバーに刷新(ドラッグで移動可能)
- チップのワンクリックでグループを一括ON/OFF

## v2.0.0
- サイドバーへのDOM挿入を廃止し、フローティングUIへ移行
  (GCal内部フレームワークの差分レンダリングとの衝突を根本解決)

## v1.x
- 初版: サイドバー挿入型のグループパネル
- カレンダー候補のキャッシュ、絞り込み、GCalのイベント処理との競合対策
